# 🧠 ПРОМПТ ДЛЯ CLAUDE CODE — MVP «ЧекМудрец»

> Скопируй всё ниже и вставь в Claude Code как одно сообщение.
> После генерации проверь .env, установи зависимости и запусти.

---

## ЗАДАЧА

Ты — senior Python-разработчик с 10-летним опытом. Создай **полностью рабочий Telegram-бот** «ЧекМудрец» — авто-трекер расходов с распознаванием чеков по фото и ИИ-советами по экономии.

Бот должен быть **готов к запуску** после `pip install` и заполнения `.env`.

---

## АРХИТЕКТУРА ПРОЕКТА

```
checkmudrets-bot/
├── bot.py              # Точка входа, запуск бота
├── handlers/
│   ├── __init__.py
│   ├── start.py        # /start, /help, приветствие
│   ├── receipt.py      # Приём фото чека, распознавание, сохранение
│   ├── stats.py        # /stats — статистика расходов
│   ├── advice.py       # /advice — персональные советы по экономии
│   ├── history.py      # /history — последние 10 чеков
│   └── report.py       # /report — еженедельный отчёт
├── services/
│   ├── __init__.py
│   ├── ocr.py          # Распознавание чека через Claude Vision API
│   ├── categorizer.py  # Категоризация позиций через Claude API
│   ├── advisor.py      # Генерация советов по экономии через Claude API
│   └── reporter.py     # Формирование текстового отчёта
├── database/
│   ├── __init__.py
│   ├── models.py       # SQLAlchemy модели (User, Receipt, Item)
│   ├── db.py           # Инициализация БД, get_session
│   └── queries.py      # CRUD-операции (добавить чек, получить статистику и т.д.)
├── utils/
│   ├── __init__.py
│   ├── config.py       # Загрузка переменных из .env
│   └── formatting.py   # Форматирование сообщений, эмодзи, markdown
├── .env.example        # Шаблон переменных окружения
├── requirements.txt    # Зависимости
├── README.md           # Документация проекта
└── .gitignore          # Стандартный Python + .env
```

---

## ТЕХНОЛОГИЧЕСКИЙ СТЕК

| Компонент | Технология | Почему |
|-----------|-----------|--------|
| Telegram-бот | `aiogram 3.x` | Асинхронный, современный, активно поддерживается |
| OCR + парсинг чека | `Anthropic Claude API` (модель claude-sonnet-4-20250514 с vision) | Отправляем фото чека → получаем структурированный JSON. Один вызов заменяет OCR + парсинг |
| Категоризация + советы | `Anthropic Claude API` | Анализ привычек, генерация персональных советов на русском |
| База данных | `SQLite` + `SQLAlchemy 2.0` (async через `aiosqlite`) | Просто, не нужен сервер, достаточно для MVP |
| Конфиг | `python-dotenv` | Переменные окружения из .env |
| HTTP-клиент | `anthropic` (официальный SDK) | Для вызовов Claude API |

---

## МОДЕЛИ БАЗЫ ДАННЫХ

```python
# database/models.py

class User:
    id: int (PK, autoincrement)
    telegram_id: int (unique, indexed)
    username: str (nullable)
    first_name: str (nullable)
    created_at: datetime (default=now)
    receipts_count: int (default=0)

class Receipt:
    id: int (PK, autoincrement)
    user_id: int (FK -> User.id)
    store_name: str           # Название магазина
    date: date                # Дата покупки (из чека)
    total: float              # Общая сумма
    raw_text: text            # Сырой распознанный текст (для дебага)
    created_at: datetime
    items: relationship -> Item[]

class Item:
    id: int (PK, autoincrement)
    receipt_id: int (FK -> Receipt.id)
    name: str                 # Название товара
    quantity: float           # Количество
    price: float              # Цена за единицу
    total: float              # Итого за позицию
    category: str             # Категория (еда, транспорт, кофе, и т.д.)
```

---

## ЛОГИКА КАЖДОГО МОДУЛЯ

### 1. `bot.py` — Точка входа

- Загрузить конфиг из .env
- Инициализировать Bot и Dispatcher (aiogram 3)
- Подключить все роутеры из handlers/
- При старте: инициализировать БД (создать таблицы если не существуют)
- Запустить polling

### 2. `handlers/start.py` — Команда /start и /help

При `/start`:
```
👋 Привет! Я ЧекМудрец — твой ИИ-помощник по расходам.

📸 Отправь мне фото чека — я распознаю все покупки за секунды.

Что я умею:
• 📸 Фото чека → мгновенное распознавание
• 📊 /stats — статистика расходов
• 💡 /advice — советы, где сэкономить
• 📋 /history — последние чеки
• 📈 /report — недельный отчёт

Просто сфоткай чек и отправь мне!
```

- Зарегистрировать пользователя в БД (если не существует)

### 3. `handlers/receipt.py` — Приём фото чека (ГЛАВНЫЙ ХЕНДЛЕР)

**Триггер:** пользователь отправляет фото (message с content_type photo)

**Логика (пошагово):**

1. Отправить сообщение-заглушку: "🔍 Распознаю чек... подожди 3–5 секунд"
2. Скачать фото в максимальном разрешении (file_id последнего photo size)
3. Конвертировать в base64
4. Вызвать `services/ocr.py` → отправить в Claude Vision API
5. Получить JSON с данными чека
6. Вызвать `services/categorizer.py` → категоризировать позиции
7. Сохранить в БД: Receipt + Items
8. Отправить пользователю красивый ответ:

```
✅ Чек распознан!

🏪 Магазин: Пятёрочка
📅 Дата: 25.03.2026
💰 Итого: 1 847 руб.

📝 Позиции:
• 🍞 Хлеб белый — 65 руб. [Продукты]
• 🥛 Молоко 1л — 89 руб. [Продукты]
• ☕ Кофе растворимый — 459 руб. [Кофе]
• 🧴 Шампунь — 312 руб. [Гигиена]
• 🍕 Пицца замороженная — 289 руб. [Продукты]
• 🧃 Сок апельсиновый — 149 руб. [Напитки]
• 🍬 Шоколад — 129 руб. [Сладости]
• 🧻 Салфетки — 89 руб. [Бытовое]
• 🫒 Масло оливковое — 266 руб. [Продукты]

💡 Совет: Кофе растворимый стоит 459 руб. Попробуй молотый — в среднем дешевле на 30%.

📊 Всего в этом месяце: 12 480 руб. (14 чеков)
```

**Обработка ошибок:**
- Если не удалось распознать → "😕 Не удалось распознать чек. Попробуй сделать фото ровнее и при хорошем освещении."
- Если фото не чека (еда, селфи и т.д.) → "🤔 Это не похоже на чек. Отправь фото кассового чека."

### 4. `services/ocr.py` — Распознавание чека через Claude Vision

**Один вызов Claude API заменяет OCR + парсер:**

Отправить изображение (base64) в Claude claude-sonnet-4-20250514 с промптом:

```
Ты — система распознавания кассовых чеков. Проанализируй фото чека и верни ТОЛЬКО валидный JSON без markdown-разметки.

Формат ответа:
{
  "success": true,
  "store_name": "Название магазина",
  "date": "YYYY-MM-DD",
  "items": [
    {
      "name": "Название товара",
      "quantity": 1.0,
      "price": 100.00,
      "total": 100.00
    }
  ],
  "total": 500.00
}

Правила:
- Если чек нечитаемый или это не чек — верни {"success": false, "reason": "описание проблемы"}
- Дата в формате YYYY-MM-DD. Если год не виден — подставь текущий 2026
- Цены в рублях, float с двумя знаками
- Название товара — как на чеке, но читаемо (расшифруй сокращения, если понятно)
- quantity по умолчанию 1.0, если не указано
- total = quantity × price (пересчитай, если не совпадает)
- Если итого не видно — посчитай сумму items
```

Парсим JSON из ответа. Если `success: false` — возвращаем ошибку.

### 5. `services/categorizer.py` — Категоризация позиций

**Вызов Claude API с промптом:**

```
Ты — система категоризации товаров из кассовых чеков. Для каждого товара определи категорию.

Доступные категории:
Продукты, Молочные, Мясо, Овощи/Фрукты, Хлеб, Напитки, Алкоголь, Кофе, Сладости, Снеки, Готовая еда, Доставка еды, Кафе/Рестораны, Бытовая химия, Гигиена, Косметика, Одежда, Обувь, Электроника, Техника, Канцелярия, Книги, Игрушки, Зоотовары, Лекарства, Спорт, Транспорт, Топливо, Подписки, Развлечения, Связь/Интернет, ЖКХ, Ремонт, Мебель, Другое

Вход (JSON):
[{"name": "Молоко 1л", "price": 89}, {"name": "Бензин АИ-95", "price": 2800}]

Ответ — ТОЛЬКО JSON без markdown:
[{"name": "Молоко 1л", "category": "Молочные"}, {"name": "Бензин АИ-95", "category": "Топливо"}]
```

**Оптимизация:** Объединить OCR + категоризацию в один вызов Claude API. В промпте OCR добавить поле `category` в items. Это сэкономит один API-вызов и ускорит обработку. Если так — файл categorizer.py становится запасным (вызывается только если OCR-промпт не вернул категории).

### 6. `services/advisor.py` — Советы по экономии

**Вызов Claude API с историей трат пользователя:**

```
Ты — финансовый советник, помогающий людям экономить деньги. Проанализируй расходы пользователя и дай 3–5 конкретных советов.

Расходы за последние 30 дней:
{json_с_расходами_по_категориям}

Правила:
- Каждый совет должен содержать конкретную сумму экономии
- Советы должны быть реалистичными и применимыми в России
- Тон — дружелюбный, без нравоучений
- Формат: эмодзи + совет + сумма
- Если данных мало (менее 5 чеков) — скажи, что нужно больше данных для точного анализа, но дай общие советы

Пример хорошего совета:
☕ Ты покупаешь кофе 12 раз в месяц на ~3 600 руб. Термос с домашним кофе сэкономит ~2 800 руб./мес.

Пример плохого совета:
"Тратьте меньше" — слишком абстрактно.
```

### 7. `handlers/stats.py` — Команда /stats

Вывести статистику за текущий месяц:

```
📊 Статистика за март 2026

💰 Всего потрачено: 34 520 руб.
🧾 Чеков отсканировано: 23

📂 По категориям:
• 🍞 Продукты — 12 400 руб. (36%)
• ☕ Кофе — 4 500 руб. (13%)
• 🍕 Кафе/Рестораны — 3 800 руб. (11%)
• 🧴 Гигиена — 2 100 руб. (6%)
• 🚗 Транспорт — 3 200 руб. (9%)
• 🎮 Развлечения — 2 800 руб. (8%)
• 📦 Другое — 5 720 руб. (17%)

📈 Средний чек: 1 501 руб.
🔝 Самая крупная покупка: 4 200 руб. (Электроника)
```

Данные берутся из БД через `database/queries.py`.

### 8. `handlers/history.py` — Команда /history

Последние 10 чеков:

```
📋 Последние чеки:

1. 🏪 Пятёрочка — 1 847 руб. (25.03)
2. 🏪 Wildberries — 3 200 руб. (24.03)
3. 🏪 Яндекс.Лавка — 890 руб. (23.03)
...

Всего за месяц: 23 чека на 34 520 руб.
```

### 9. `handlers/report.py` — Команда /report

Еженедельный отчёт (ручной вызов через /report, позже можно автоматизировать по крону):

Собрать данные за последние 7 дней → отправить в `services/reporter.py` → сгенерировать отчёт → отправить пользователю.

```
📈 Отчёт за неделю (18–25 марта)

💰 Потрачено: 8 740 руб.
📊 На 12% больше, чем на прошлой неделе

🏆 Топ категорий:
1. Продукты — 3 200 руб.
2. Кафе — 2 100 руб.
3. Транспорт — 1 400 руб.

💡 Советы на следующую неделю:
• 🍕 Ты заказывал доставку 3 раза (1 800 руб.). Готовь дома — сэкономишь ~1 200 руб.
• ☕ 5 кофе по 350 руб. = 1 750 руб. Бери с собой — экономия ~1 400 руб./нед.
```

### 10. `utils/config.py`

```python
from dotenv import load_dotenv
import os

load_dotenv()

BOT_TOKEN = os.getenv("BOT_TOKEN")            # Telegram Bot Token
ANTHROPIC_API_KEY = os.getenv("ANTHROPIC_API_KEY")  # Anthropic API Key
DATABASE_URL = os.getenv("DATABASE_URL", "sqlite+aiosqlite:///checkmudrets.db")
CLAUDE_MODEL = os.getenv("CLAUDE_MODEL", "claude-sonnet-4-20250514")
```

### 11. `.env.example`

```
BOT_TOKEN=your_telegram_bot_token_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
DATABASE_URL=sqlite+aiosqlite:///checkmudrets.db
CLAUDE_MODEL=claude-sonnet-4-20250514
```

### 12. `utils/formatting.py`

Маппинг категорий → эмодзи:

```python
CATEGORY_EMOJI = {
    "Продукты": "🍞", "Молочные": "🥛", "Мясо": "🥩",
    "Овощи/Фрукты": "🥬", "Хлеб": "🍞", "Напитки": "🧃",
    "Алкоголь": "🍷", "Кофе": "☕", "Сладости": "🍬",
    "Снеки": "🍿", "Готовая еда": "🥡", "Доставка еды": "🛵",
    "Кафе/Рестораны": "🍕", "Бытовая химия": "🧹", "Гигиена": "🧴",
    "Косметика": "💄", "Одежда": "👕", "Обувь": "👟",
    "Электроника": "📱", "Техника": "🔧", "Канцелярия": "📎",
    "Книги": "📚", "Игрушки": "🧸", "Зоотовары": "🐾",
    "Лекарства": "💊", "Спорт": "🏋️", "Транспорт": "🚗",
    "Топливо": "⛽", "Подписки": "📺", "Развлечения": "🎮",
    "Связь/Интернет": "📡", "ЖКХ": "🏠", "Ремонт": "🔨",
    "Мебель": "🛋️", "Другое": "📦"
}
```

Функция для форматирования суммы: `1847.50` → `1 847 руб.`
Функция для процентов: генерация прогресс-бара из блоков.

---

## REQUIREMENTS.TXT

```
aiogram>=3.4.0
anthropic>=0.40.0
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
python-dotenv>=1.0.0
Pillow>=10.0.0
```

---

## .GITIGNORE

```
.env
__pycache__/
*.pyc
*.db
.idea/
.vscode/
venv/
.venv/
```

---

## README.MD — ДОКУМЕНТАЦИЯ

Напиши полноценный README.md со следующей структурой:

### Структура README:

1. **Заголовок + бейджи**: Название «ЧекМудрец», описание в одну строку, бейджи (Python 3.11+, aiogram 3, Claude API, License MIT)

2. **О проекте**: 3–4 предложения — что делает бот, какую проблему решает, для кого

3. **Возможности** (фича-лист с эмодзи):
   - 📸 Распознавание чеков по фото (даже без QR)
   - 🤖 ИИ-категоризация по 35+ категориям
   - 💡 Персональные советы по экономии
   - 📊 Статистика расходов по категориям
   - 📈 Еженедельные отчёты
   - 📋 История чеков

4. **Быстрый старт** (пошагово):
   - Клонирование репо
   - Создание виртуального окружения
   - pip install -r requirements.txt
   - Копирование .env.example → .env
   - Заполнение токенов
   - python bot.py

5. **Команды бота** (таблица):
   | Команда | Описание |
   | /start | Начало работы |
   | /stats | Статистика за месяц |
   | /advice | Советы по экономии |
   | /history | Последние чеки |
   | /report | Недельный отчёт |
   | /help | Справка |
   | 📸 Фото | Отправить фото чека |

6. **Архитектура**: текстовая схема модулей (handlers → services → database)

7. **Технологии**: таблица с технологиями и версиями

8. **Переменные окружения**: описание каждой переменной из .env

9. **Структура проекта**: дерево файлов

10. **Скриншоты / примеры**: placeholder — "Примеры работы бота будут добавлены после запуска"

11. **Roadmap**: что будет дальше (семейный аккаунт, веб-дашборд, iOS, интеграция с банками)

12. **Команда**: Земсков А.В., Якимов Н.Н., Викторов А.Д., Демидов И.С., Анисимов А.А. — Группа ЭФБО-06-23

13. **Лицензия**: MIT

---

## ВАЖНЫЕ ТРЕБОВАНИЯ К КОДУ

1. **Весь код — асинхронный** (async/await). Никаких синхронных блокировок.
2. **Type hints** везде. Используй `from __future__ import annotations` где нужно.
3. **Обработка ошибок**: каждый API-вызов обёрнут в try/except. Пользователь никогда не видит трейсбек. При ошибке — понятное сообщение.
4. **Логирование**: `import logging`, уровень INFO для основных событий, ERROR для ошибок. Логировать каждый распознанный чек (user_id, сумма, кол-во позиций).
5. **Один API-вызов для OCR + категоризация**: объедини распознавание и категоризацию в один промпт Claude Vision. Categorizer.py — fallback.
6. **Парсинг JSON из Claude**: Claude может вернуть JSON в markdown-блоке (```json...```). Всегда чисти ответ перед json.loads().
7. **Комментарии в коде** — на русском языке.
8. **Модульность**: каждый файл — одна ответственность. Не больше 150 строк на файл.
9. **Graceful shutdown**: корректное завершение при Ctrl+C.
10. **Rate limiting**: не чаще 1 фото в 5 секунд от одного пользователя (простой cooldown).

---

## ЗАПУСК И ТЕСТ

После генерации всех файлов:

```bash
cd checkmudrets-bot
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
cp .env.example .env
# Заполни .env своими токенами
python bot.py
```

Бот должен запуститься, ответить на /start и принять фото чека.

---

## ФИНАЛЬНАЯ ПРОВЕРКА

После написания всего кода — проверь:

- [ ] `bot.py` запускается без ошибок
- [ ] /start отвечает приветственным сообщением и регистрирует пользователя
- [ ] Фото чека обрабатывается и возвращает распознанные позиции
- [ ] /stats показывает статистику (или сообщение "пока нет данных")
- [ ] /advice даёт советы (или просит отсканировать больше чеков)
- [ ] /history показывает список чеков
- [ ] /report генерирует отчёт
- [ ] README.md полный и красиво отображается на GitHub
- [ ] .gitignore включает .env и __pycache__
- [ ] Нет хардкода токенов в коде

---

Создай все файлы. Начни с `database/models.py` и `database/db.py`, затем `services/`, затем `handlers/`, затем `bot.py`, и в конце `README.md`. Пиши сразу рабочий код, не заглушки.
